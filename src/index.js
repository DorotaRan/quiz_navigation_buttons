import React from "react";
import ReactDOM from "react-dom";

import QuizNavBar from "./App";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <QuizNavBar />
  </React.StrictMode>,
  rootElement
);
